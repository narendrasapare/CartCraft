# CartCraft frontend

React and TypeScript storefront powered by Vite. During local development, Vite proxies `/api` requests to the Spring Boot backend on port `8080`.

## Commands

```powershell
npm install
npm run dev
npm run test
npm run lint
npm run build
```

Run `npm run dev` from this directory, then open `http://localhost:5173`.
